# Frontend — КИБЕР РЫВОК

Frontend часть проекта **КИБЕР РЫВОК** — симулятора живого кибер‑города, где AI‑агенты общаются, запоминают события и
меняют отношение друг к другу в реальном времени.

## 🚀 Технологический стек

- **React 19** — библиотека для построения пользовательских интерфейсов
- **Vite 7** — быстрый сборщик и dev‑сервер
- **React Router DOM 6** — маршрутизация в SPA
- **Zustand 4** — легковесное управление состоянием
- **Framer Motion 11** — анимации и переходы
- **React Force Graph 2D** — визуализация графа отношений
- **CSS Modules** — модульные стили для компонентов

## 📁 Структура проекта

```
frontend/
├── src/
│   ├── components/          # Переиспользуемые UI‑компоненты
│   │   ├── AddAgentForm/   # Форма создания нового агента
│   │   ├── AgentCard/      # Карточка агента (превью)
│   │   ├── AgentGrid/      # Сетка агентов на дашборде
│   │   ├── AgentProfile/   # Детальный профиль агента
│   │   ├── AuthWrapper/    # Обёртка для защиты приватных маршрутов
│   │   ├── EventStream/    # Поток событий симуляции
│   │   ├── GlobalActionPanel/ # Панель глобальных действий
│   │   ├── MemoryList/     # Список воспоминаний агента
│   │   ├── MessageInput/   # Поле ввода сообщений
│   │   ├── Navbar/         # Навигационная панель
│   │   ├── RelationGraph/  # Граф отношений между агентами
│   │   └── TimeControlPanel/ # Панель управления временем симуляции
│   │
│   ├── pages/              # Страницы приложения
│   │   ├── MainPage/       # Главная страница (лендинг)
│   │   ├── LoginPage/      # Страница входа
│   │   ├── RegisterPage/   # Страница регистрации
│   │   ├── DashboardPage/  # Дашборд с агентами
│   │   ├── AgentPage/      # Профиль агента (детальный)
│   │   ├── GroupChatPage/  # Групповые чаты
│   │   ├── RelationsPage/  # Визуализация графа отношений
│   │   ├── SettingsPage/   # Настройки приложения
│   │   └── NotFoundPage/   # Страница 404
│   │
│   ├── services/           # Слой коммуникации с backend
│   │   ├── api.js         # REST API клиент (HTTP запросы)
│   │   ├── websocket.js   # WebSocket клиент (реал‑тайм события)
│   │   └── simulation.js  # Управление симуляцией
│   │
│   ├── store/              # Zustand сторы (глобальное состояние)
│   │   ├── agentStore.js  # Состояние агентов
│   │   ├── eventStore.js  # Состояние событий
│   │   ├── simulationStore.js # Состояние симуляции
│   │   └── errorStore.js  # Состояние ошибок
│   │
│   ├── styles/             # Глобальные стили
│   │   ├── variables.css  # CSS переменные (цвета, размеры)
│   │   ├── global.css     # Глобальные стили (body, html)
│   │   └── utilities.css  # Утилитарные классы
│   │
│   ├── types/              # Типы данных (JSDoc)
│   │   ├── Agent.js
│   │   ├── Event.js
│   │   └── Relation.js
│   │
│   ├── App.jsx             # Корневой компонент с роутингом
│   ├── main.jsx           # Точка входа приложения
│   └── index.css          # Главный файл стилей
│
├── assets/                 # Статические ресурсы (изображения, иконки)
├── index.html             # HTML шаблон
├── package.json           # Зависимости и скрипты
├── vite.config.js         # Конфигурация Vite
└── eslint.config.js       # Конфигурация ESLint
```

## 🛠 Установка и запуск

### Предварительные требования

- **Node.js** версии 18 или выше
- **npm** или **yarn**
- Запущенный backend сервер (см. [README проекта](../README.md))

### Установка зависимостей

```bash
cd frontend
npm install
```

### Переменные окружения

Создайте файл `.env` (или `.env.local`) в папке `frontend/`:

```env
# Базовый URL для REST API
VITE_API_BASE_URL=http://localhost:8000

# Базовый URL для WebSocket соединений
VITE_WS_BASE_URL=ws://localhost:8000
```

**Примечание:** Если переменные не указаны, используются значения по умолчанию:

- `VITE_API_BASE_URL` → `http://localhost:8000`
- `VITE_WS_BASE_URL` → `ws://localhost:8000`

### Запуск dev‑сервера

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173` (или другому свободному порту, который выберет Vite).

### Сборка для production

```bash
npm run build
```

Собранные файлы будут находиться в папке `dist/`.

### Просмотр production сборки

```bash
npm run preview
```

### Линтинг кода

```bash
npm run lint
```

## 📄 Страницы приложения

### Публичные страницы

- **`/`** — **MainPage** — лендинг с описанием проекта
- **`/login`** — **LoginPage** — страница входа в систему
- **`/register`** — **RegisterPage** — страница регистрации нового пользователя

### Приватные страницы (требуют авторизации)

- **`/dashboard`** — **DashboardPage** — главный дашборд:
    - Список всех агентов пользователя
    - Отображение настроения и энергии каждого агента
    - Панель управления временем симуляции
    - Создание новых агентов

- **`/agent/:id`** — **AgentPage** — детальный профиль агента:
    - Память агента (воспоминания из векторной БД)
    - Планы агента
    - История взаимодействий
    - Чат с агентом (отправка сообщений)

- **`/group-chat`** — **GroupChatPage** — групповые чаты:
    - Создание групповых чатов
    - Добавление/удаление агентов в чат
    - Просмотр сообщений агентов в реальном времени
    - Отправка сообщений в чат

- **`/relations`** — **RelationsPage** — визуализация графа отношений:
    - Интерактивный граф всех отношений между агентами
    - Цвет рёбер отражает уровень симпатии/антипатии
    - Граф обновляется в реальном времени

- **`/settings`** — **SettingsPage** — настройки приложения:
    - Управление параметрами симуляции
    - Настройки интерфейса

- **`*`** — **NotFoundPage** — страница 404 для несуществующих маршрутов

## 🔐 Аутентификация

Приложение использует JWT токены для аутентификации:

1. При успешном входе (`/login`) или регистрации (`/register`) токен сохраняется в `localStorage`
2. Компонент `AuthWrapper` проверяет наличие токена для приватных маршрутов
3. При отсутствии токена пользователь перенаправляется на `/login`
4. Токен автоматически добавляется в заголовок `Authorization: Bearer <token>` для всех API запросов
5. При получении 401 ошибки токен удаляется и происходит редирект на `/login`

## 📦 Управление состоянием (Zustand)

Приложение использует **Zustand** для управления глобальным состоянием:

### `agentStore.js`

Управляет состоянием агентов:

- Список всех агентов
- Загрузка агентов с сервера
- Создание новых агентов
- Обновление информации об агентах

### `eventStore.js`

Управляет событиями симуляции:

- Поток событий в реальном времени (через WebSocket)
- История событий
- Фильтрация событий

### `simulationStore.js`

Управляет состоянием симуляции:

- Статус симуляции (запущена/остановлена)
- Скорость симуляции
- Управление симуляцией (пауза, изменение скорости)

### `errorStore.js`

Управляет ошибками приложения:

- Стек ошибок
- Отображение ошибок пользователю
- Автоматическая очистка старых ошибок

## 🌐 Коммуникация с backend

### REST API (`services/api.js`)

Универсальная функция `request()` для HTTP запросов:

- Автоматически добавляет JWT токен в заголовки
- Обрабатывает ошибки и пушит их в `errorStore`
- При 401 ошибке удаляет токен и перенаправляет на `/login`

Примеры использования:

```javascript
import { request } from '../services/api'

// GET запрос
const agents = await request('/api/agents')

// POST запрос
const newAgent = await request('/api/agents', {
  method: 'POST',
  body: JSON.stringify({ name: 'Agent', ... })
})
```

### WebSocket (`services/websocket.js`)

Подключение к WebSocket для получения событий в реальном времени:

- Автоматическое переподключение при разрыве соединения
- Подписка на события симуляции
- Обновление `eventStore` при получении новых событий

## 🎨 Стилизация

### CSS Modules

Каждый компонент и страница имеют свой CSS модуль:

- `ComponentName.jsx` → `ComponentName.module.css`
- Стили изолированы и не конфликтуют между компонентами
- Импорт: `import styles from './ComponentName.module.css'`

### Глобальные стили

- **`variables.css`** — CSS переменные (цвета, размеры, отступы)
- **`global.css`** — глобальные стили для `body`, `html`, базовых элементов
- **`utilities.css`** — утилитарные классы для переиспользования

### Анимации

Используется **Framer Motion** для плавных анимаций и переходов между страницами.

## 🐳 Docker

Для запуска через Docker см. основной [README проекта](../README.md).

## 📝 Разработка

### Добавление новой страницы

1. Создайте папку в `src/pages/YourPage/`
2. Создайте компонент `YourPage.jsx` и стили `YourPage.module.css`
3. Добавьте маршрут в `App.jsx`:
   ```jsx
   <Route path="/your-page" element={<YourPage />} />
   ```
4. Если страница приватная, оберните в `AuthWrapper`:
   ```jsx
   <Route path="/your-page" element={
     <AuthWrapper>
       <YourPage />
     </AuthWrapper>
   } />
   ```

### Добавление нового компонента

1. Создайте папку в `src/components/YourComponent/`
2. Создайте `YourComponent.jsx` и `YourComponent.module.css`
3. Экспортируйте компонент и используйте где необходимо

### Добавление нового стора (Zustand)

1. Создайте файл в `src/store/yourStore.js`:
   ```javascript
   import { create } from 'zustand'
   
   const useYourStore = create((set) => ({
     data: null,
     setData: (data) => set({ data }),
   }))
   
   export default useYourStore
   ```
2. Используйте в компонентах:
   ```javascript
   import useYourStore from '../store/yourStore'
   
   function Component() {
     const { data, setData } = useYourStore()
     // ...
   }
   ```

## 🔍 Отладка

### React DevTools

Установите расширение [React Developer Tools](https://react.dev/learn/react-developer-tools) для браузера.

### Zustand DevTools

Для отладки Zustand сторов можно
использовать [Redux DevTools Extension](https://github.com/reduxjs/redux-devtools-extension).

### Консоль браузера

Все ошибки API и WebSocket логируются в консоль браузера. Проверяйте вкладку **Console** в DevTools.

## 📚 Дополнительные ресурсы

- [React документация](https://react.dev/)
- [Vite документация](https://vite.dev/)
- [React Router документация](https://reactrouter.com/)
- [Zustand документация](https://docs.pmnd.rs/zustand)
- [Framer Motion документация](https://www.framer.com/motion/)

## 🤝 Вклад в проект

При разработке следуйте существующим паттернам:

- Используйте CSS Modules для стилей
- Используйте Zustand для глобального состояния
- Используйте `request()` из `services/api.js` для API запросов
- Добавляйте JSDoc комментарии для типов и функций
- Следуйте структуре папок проекта

---

**КИБЕР РЫВОК** — симулятор живого кибер‑города с AI‑агентами
